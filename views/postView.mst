{{> header}}

        <div class="postV">
          <h2>{{post.title}}</h2><br />
            <hr />
          <figure>
              <img src="/media/{{post.photo}}" alt="{{post.title}}">
          </figure>
          <article>
            <p>{{{post.body}}}</p>
              <hr />
            <p class="tags">Tags: <br />{{post.tags}}</p>
            <br /><br />
            <a href="/post/{{post.slug}}/edit">[ Editar ]</a>
          </article>
        </div>  

  </body>
</html>
